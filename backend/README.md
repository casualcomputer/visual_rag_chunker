# Chunking Visualization Backend

FastAPI backend for Markdown chunking visualization.

Run locally:

```bash
uv sync --frozen
uv run uvicorn app:app --reload --port 8000
```

Recommended Python version: `3.11` (see `.python-version`).
